services:

  web:
    image: nginx:alpine
    ports:
      - "8081:80"
    volumes:
      # Serve the repo root (index.html + data/ + images/ + videos/) as static files.
      - ./:/usr/share/nginx/html:ro
    restart: unless-stopped
